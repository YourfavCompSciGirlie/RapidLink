{
  "name": "rapidlink",
  "version": "0.1.0",
  "private": true,
  "description": "Tshwane RapidLink emergency-response platform",
  "engines": { "node": ">=22.0.0", "npm": ">=11.0.0" },
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently --kill-others --names shared,backend,frontend --prefix-colors magenta,cyan,blue \"npm run dev --workspace=@rapidlink/shared\" \"npm run dev --workspace=@rapidlink/backend\" \"npm run dev --workspace=@rapidlink/frontend\"",
    "build": "npm run build --workspace=@rapidlink/shared && npm run build --workspace=@rapidlink/backend && npm run build --workspace=@rapidlink/frontend",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.44.0",
    "@typescript-eslint/parser": "^8.44.0",
    "concurrently": "^9.2.1",
    "eslint": "^8.57.1",
    "typescript": "^5.9.2"
  },
  "packageManager": "npm@11.4.2"
}
